# Daily Operations

```sh
zmorning --brief
zdots-ctl status
capabilities --json
agent-guide
```

If a service is unexpectedly down:

1. Check `zdots-ctl status`.
2. Check the relevant manpage.
3. Check `docs/troubleshooting.md`.
4. File `zdots-issue` if behavior contradicts docs or `--help`.
