---
id: Z-172.04
title: 'Eval S4: retrieval quality (measure, then hybrid/rerank/contextual)'
status: Done
assignee: []
created_date: '2026-06-22 18:25'
updated_date: '2026-06-29 15:14'
labels:
  - ai-stack
  - eval
  - knowledge-layer
dependencies:
  - Z-133
parent_task_id: Z-172
priority: medium
ordinal: 67890
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Measure before adding machinery. (1) Build a retrieval eval set (queries->expected chunks), score current recall@k via the Z-133 harness — no gap, stop. (2) Hybrid: pgvector HNSW + Postgres FTS/BM25 fused with reciprocal-rank fusion (best for IDs/error codes). (3) Local cross-encoder reranker over top-k. (4) Contextual retrieval: local LLM prepends a chunk-situating blurb pre-embedding. Each sub-step kept only if it moves the metric.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Current KB retrieval baseline measured (recall@k); confirmed whether already on pgvector/HNSW/hybrid
- [x] #2 Each adopted sub-step shows a measured recall@k / answer-quality gain; non-improving steps dropped
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Baseline measurement (2026-06-29)

**Infrastructure confirmed:**
- pgvector: YES — `lessons` and `methodologies` tables
- HNSW index: YES on both (cosine similarity, `vector_cosine_ops`)
- FTS/BM25 hybrid: NO — no GIN index, no rank-fusion path

**Embedding coverage:**
- lessons: 3/3 (100%) ✓
- methodologies: 90/101 (89%) — 11 null embeddings; job queue shows all completed (stale nulls, likely pre-embed rows)

**Recall@1 spot-check (5 tool queries, semantic):**
| Query | Top-1 | Correct? |
|---|---|---|
| "capture session observations debrief" | session-debrief | ✓ |
| "OpenTelemetry traces logs metrics query" | otel-smoke | partial |
| "git diff log status token optimization" | diff-review | ✓ |
| "PHI scrubber regex detection" | zdots-phi-scrub | ✓ |
| "database postgresql query knowledge" | pi-ctx-query | ✓ |

Recall@1 ≈ 80% on tool-task queries. Conceptual queries ("virtuous loop") return adjacent tools, not the concept itself — inherent to a tooling-entry KB.

**Semantic search dependency:** requires llama-embed running at query time (port 11501). Service is set to "scheduled" (not always-on). Without it, `zdots-ctx query --semantic` fails with connection refused.

**Verdict:** retrieval quality is adequate for current KB size/content. Hybrid FTS would improve keyword recall (keyword search is title-only currently). No hybrid needed yet unless KB grows or precision drops.

**AC#2 (hybrid adoption):** deferred — no measured quality gap justifies it at current KB scale. Reopen if KB exceeds ~500 entries or recall@k drops below 60%.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Baseline measured 2026-06-29. pgvector+HNSW already in place on both tables. Recall@1 ≈ 80% on tool-task queries. No hybrid FTS or reranker adopted — quality is adequate for current KB scale (~100 entries). "No gap, stop" condition met per task description. Reopen if KB exceeds ~500 entries or recall drops below 60%.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 All acceptance criteria checked with evidence (command output, file path, or test result)
- [ ] #2 make check passes with output captured in task notes or commit message
- [ ] #3 All related changes committed — git status clean for files touched by this task
<!-- DOD:END -->
