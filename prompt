# vim:ft=zsh:

setopt prompt_subst
setopt transient_rprompt

autoload -U +X vcs_info

# PROMPT {{{

local interactive="%{$fg_bold[green]%}%{$reset_color%}"
local normal="%{$fg_bold[red]%}%{$reset_color%}"

local _prompt1="%{$fg_bold[red]%}❱%{$reset_color%}%{$fg_bold[green]%}❱%{$reset_color%}%{$fg_bold[blue]%}❱%{$reset_color%}"
local _prompt2="%{$fg_bold[red]%}↳%{$reset_color%}%{$fg_bold[green]%}↳%{$reset_color%}%{$fg_bold[blue]%}↳%{$reset_color%}"
local _prompt3="%{$fg_bold[red]%}❚%{$reset_color%}%{$fg_bold[green]%}❚%{$reset_color%}%{$fg_bold[blue]%}❚%{$reset_color%}"

PS1="%2~ $_prompt1 "
PS2=" $_prompt2 "
PS3=" $_prompt3 "

function zle-line-init zle-keymap-select {
  local mode="${${KEYMAP/vicmd/ $normal}/(main|viins)/ $interactive}"
  PS1="$mode %2~ $_prompt1 "
  zle reset-prompt
}

# }}}

# RPROMPT {{{

local git_formats_template='%r %S %b %c %u %m %8>>%i%>>'
local git_actionformats_template='(%s)-[%b|%a]'

git_unstaged () print 'U' # T
git_staged () print 'S'

git_formats () print $git_formats_template
git_actionformats () print $git_actionformats_template
git_nvcsformats () print ''

zstyle ':vcs_info:*' enable git
zstyle ':vcs_info:*+*:*' debug false
zstyle ':vcs_info:git:*:-all-' command /usr/local/bin/git

zstyle ':vcs_info:git:*' get-revision true
zstyle ':vcs_info:git:*' check-for-changes true

zstyle ':vcs_info:*' nvcsformats "$(git_nvcsformats)"

zstyle ':vcs_info:git:*:*' actionformats "$(git_actionformats)"
zstyle ':vcs_info:git:*:*' formats       "$(git_formats)"
zstyle ':vcs_info:git:*:*' stagedstr     "$(git_staged)"
zstyle ':vcs_info:git:*:*' unstagedstr   "$(git_unstaged)"

precmd () vcs_info

RPS1='${vcs_info_msg_0_}'
RPS2="$RPS1"

# }}}
