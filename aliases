# vim:ft=zsh:

# NOTE: Prefix aliased commands with a single space so they are
# automatically excluded from the command history.

# Shell and OTE configuration editing {{{

# OTE {{{

# for ote in "ote" "vi" "e" "emacs"
# do
#   alias $ote="vim"
# done
# alias mvim=gvim

alias vv=" vim $HOME/.config/nvim/init.vim"

# }}}

alias zz=" vim -p $ZDOTDIR/.zshrc $ZDOTDIR/.zshenv $ZDOTDIR/aliases"

alias zshrc="vim /Users/mike/.config/zsh/.zshrc"
alias zshenv="vim /Users/mike/.config/zsh/.zshenv"

alias vali="vim $ZDOTDIR/aliases"

alias alg="alias | grep -i"

alias xdots=" ~xdots" # TODO: Lump these in with nocorrect loop
alias vdots=" ~vdots"
alias zdots=" ~zdots"

# }}}

# Ignore from history {{{

for cmd in "exit" "mkdir" "rm" "rvm" "source"
do
  alias $cmd=" $cmd"
done

# }}}

# Default action aliases {{{

# Associate these filetypes with Vim as their
# default action.
for ftype in coffee css erb haml html ru lock txt vim
do
  alias -s $ftype=vim
done

for ftype in markdown md
do
  alias -s $ftype=marked
done

# }}}

# Git {{{

alias g=" git"
# git pull
alias gpr="git pull --rebase --autostash"
# git commit
alias gc=" git commit --verbose"
alias gcm" git commit -m"
alias gcA=" git add -A"
alias gcam=" git commit --verbose --amend"
# git add
alias ga=" git add"
alias gaa=" git add --all"
alias gapa=" git add --patch"
# git branch
alias gb=" git branch"
# --all List both remote-tracking branches and local branches.
alias gba=" git branch --all"
# # git clone
# alias gcl=" git clone"
# git fetch
alias gfa=" git fetch --all --prune"
# git status
alias gss=" git status --short"
# git diff
alias gwd=" git diff --word-diff"
# git checkout
alias gco=' git checkout'
# -b create and checkout a new branch
alias gcb=" git checkout -b"
alias branch\?="git rev-parse --abbrev-ref HEAD"

# }}}

# ls {{{

# -A List all entries except for . and ...
# -G Enable colorized output. This option is equivalent to defining CLICOLOR in the environment.
# -p Write a slash (`/') after each filename if that file is a directory.
# -B Force printing of non-printable characters (as defined by ctype(3) and current locale settings) in file names as \xxx, where xxx is the numeric value of the character in octal.
alias ls="gls --color=auto"
# -C Force multi-column output; this is the default when output is to a terminal.
alias la="ls -A -C"
# -o List in long format, but omit the group id.
alias ll="ls -A -o -h"

# }}}

# dirs {{{

# -v number the directories in the stack when printing.
# -l print directory names in full instead of using of using ~ expressions
alias dh='dirs -v -l'

# }}}

# Homebrew {{{

alias bsl=" brew services list"
alias bstart=" brew services start"
alias bstop=" brew services stop"

# }}}

# ! aliases {{{

alias clear!="klear"

# }}}

# Global Aliases {{{

alias -g gi=" | grep -i "

# }}}

# Uncategorized

alias -- -="cd -"
alias -g ...='../..'
alias ext="exit"
alias rmed="rm -d ./**/*(/^F)"
# alias xx="atool -x"
alias xx="exit"
alias gfp="git format-patch HEAD~1 --no-color --stdout"
alias chrome="open -a \"Google Chrome\""
# alias spotify="open -a \"Spotify\""
alias safari="open -a \"Safari\""
alias firefox="open -a \"FirefoxDeveloperEdition\""
alias marked="open -a \"Marked 2\""
alias vcsi='vcs_info command; vcs_info_lastmsg'
alias fl="functions | grep -i \"^\w\+\s\+()\""

alias zerologs='sudo gtruncate -s 0 **.log'
alias tailogs='tail -F **.log'
alias iso8601="date -u +\"%Y-%m-%dT%H:%M:%SZ\""

alias -g uniquelines=' | uniq -c | sort -n'
alias reload="bounce" # clobber ; bounce"
alias reload!="bounce" # clobber ; recompile ; bounce"
alias realias="source $ZDOTDIR/aliases"
alias cwd="pwd | pbcopy"
alias -g lx="l -10"
alias -g lxx="l -20"
alias -g lxxx="l -30"

# Exclude for auto-correction {{{

# Prefix these commands with `nocorrect` to silence annoying
# warnings from the correct option.
for cmd in "rspec" "mv" "~zdots" "~xdots" "~vdots" "bundle"
do
  alias $cmd="nocorrect $cmd"
done

# }}}

alias clone="git clone"

alias foobar="echo bar"

alias ww="whence -w"

alias be='bundle exec'
alias -g rc='rails console'
alias -g db='rails dbconsole'
alias -g k='rake'

alias lint-vim-settings="vint --color --style ~/.config/nvim/init.vim"
alias reri="gem rdoc --all --ri --no-rdoc"

alias grep="grep --color=auto --exclude-dir=.git -i"
alias fgrep="fgrep --color=auto --exclude-dir=.git -i"
alias egrep="egrep --color=auto --exclude-dir=.git -i"
alias ag='ag --path-to-ignore ~/.agignore'

#alias python='/usr/local/bin/python3'
#alias pip='/usr/local/bin/pip3'
alias pgviz="pbpaste | sed 's/^/EXPLAIN (ANALYZE, COSTS, VERBOSE, BUFFERS, FORMAT JSON) /' | psql -qAt wmx_rails_api_development | gocmdpev"
alias db="psql wmx_rails_api_development"

#alias scp='rsync avzP'
alias desktop="cd ~/desktop"

alias -g tls="list-sessions"

alias darken="dark-mode on"
alias lighten="dark-mode off"

alias vi="/usr/local/bin/nvim"

# git -C "$(brew --repo homebrew/dupes)" fetch --unshallow
